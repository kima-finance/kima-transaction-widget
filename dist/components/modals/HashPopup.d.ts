import React from 'react';
import { TransactionData } from '../../interface';
interface Props {
    data?: TransactionData;
}
declare const HashPopup: ({ data }: Props) => React.JSX.Element;
export default HashPopup;
